const ENVIRONMENT = import.meta.env.MODE.toLowerCase(); // development, staging, production

const [DEVELOPMENT, STAGING, PRODUCTION] = ["development", "staging", "production"];

export const Feature = {
  BETA: "beta",
};
